---
- name: Deploy Bootstrap Argo App of Apps
  environment:
    K8S_AUTH_KUBECONFIG: "{{ cluster_kubeconfig }}"
  community.kubernetes.k8s:
    state: present
    wait: yes
    definition: "{{ lookup('file', '{{ manifest_dir }}/bootstrap/argocd-app-bootstrap.yaml') | from_yaml }}"
...
